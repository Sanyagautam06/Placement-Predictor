{
  "name": "placement-predictor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "python3 app.py > startup.log 2>&1",
    "build": "echo 'No build phase needed'",
    "start": "python3 app.py",
    "lint": "echo 'No lint check needed'"
  }
}
