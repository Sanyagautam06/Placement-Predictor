{
  "name": "placement-predictor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "(python3 -m pip install --break-system-packages -r requirements.txt && python3 app.py) > startup.log 2>&1",
    "build": "echo 'No build phase needed'",
    "start": "pip3 install --break-system-packages -r requirements.txt && python3 app.py",
    "lint": "echo 'No lint check needed'"
  }
}